import { Box } from '@chakra-ui/react';
import React from 'react';

const Contact = ({ id }) => {
	return (
		<div id={id}>
			<Box h="1000px">
				<>Contact</>
			</Box>
		</div>
	);
};

export default Contact;
