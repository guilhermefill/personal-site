import { Box } from '@chakra-ui/react';
import React from 'react';

const Projects = ({ id }) => {
	return (
		<div id={id}>
			<Box h="1000px">
				<>Projects</>
			</Box>
		</div>
	);
};

export default Projects;
