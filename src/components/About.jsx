import { Box } from '@chakra-ui/react';
import React from 'react';

const About = ({ id }) => {
	return (
		<div id={id}>
			<Box h="1000px">
				<>About</>
			</Box>
		</div>
	);
};

export default About;
